#include "header.h"

void mode() {
    printf("SETS - CURSOR-BASED IMPLEMENTATION (ORDERED)\n\n");
}

bool equal(VirtualHeap VH, SET A, SET B) {
    // do something;
    return true;
}

SET Union(VirtualHeap* VH, SET A, SET B) {
    SET C;
    init(&C);

    // do something

    return C;
}

SET Intersection(VirtualHeap* VH, SET A, SET B) {
    SET C;
    init(&C);

    // do something

    return C;
}

SET Difference(VirtualHeap* VH, SET A, SET B) {
    SET C;
    init(&C);

    // do something

    return C;
}
